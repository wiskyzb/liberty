spring:
  application:
    name: liberty-demo
  cloud:
    nacos:
      config:
        file-extension: yaml # 配置内容的数据格式
        group: DEFAULT_GROUP # 组，默认为 DEFAULT_GROUP
        server-addr: 192.168.1.150:8848 # 注册中心地址
        enabled: true
        namespace: dev
      discovery:
        server-addr: 192.168.1.150:8848 # 注册中心地址

  profiles:
    active: dev
server:
  port: 8081







